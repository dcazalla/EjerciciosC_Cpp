/*
* Desarrolle un programa en C al que se le pase por línea de comando dos parámetros.
* El primero puede ser -l o -u, y el segundo, el nombre de un fichero. Si el primero de ellos es -l,
* se debe pasar a minúsculas todas las mayúsculas del fichero. Si es -u, hay que convertir a
* mayúsculas todas las minúsculas.
*/
